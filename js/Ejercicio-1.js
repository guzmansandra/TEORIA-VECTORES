// crear un vector vacio llamado colores.
// luego agrega 3 colores diferentes y recorre el vetor 

// Inicializar vector 
let colores = [];

// Agregar informacion 
colores.push("yellow", "black", "white" );  

for(let i = 0; i < colores.length; i++){
    console.log(colores[i]);
}
